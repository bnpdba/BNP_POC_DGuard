LISTENER =
   (DESCRIPTION_LIST =
      (DESCRIPTION =
         (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))
         (ADDRESS = (PROTOCOL = TCP)(HOST = {{ server_prim }})(PORT = {{ vm_LSNRPORT }}))
      )
   )

##static registration for Dataguard

SID_LIST_LISTENER =
  (SID_LIST=
    (SID_DESC=
      (GLOBAL_DBNAME={{ dbprim }}_DGMGRL)
      (ORACLE_HOME={{ oracle_home_19000 }})
      (SID_NAME={{ dbprim }})
      (ENVS = "TNS_ADMIN={{ DB_TNS_ADMIN }}")
    )
    (SID_DESC=
      (GLOBAL_DBNAME={{ dbprim }})
      (ORACLE_HOME={{ oracle_home_19000 }})
      (SID_NAME={{ dbprim }})
      (ENVS = "TNS_ADMIN={{ DB_TNS_ADMIN }}")
    )
  )

REGISTRATION_INVITED_NODES_LISTENER=()
VALID_NODE_CHECKING_REGISTRATION_LISTENER_IB=SUBNET
